{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "python customer/manage.py migrate && python customer/manage.py collectstatic --noinput && gunicorn customer.dycetix_project.wsgi:application --bind 0.0.0.0:$PORT --workers 3"
  }
}